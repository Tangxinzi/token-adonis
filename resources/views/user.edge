<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <link rel="icon" href="/logo.jpg" />
  <title>后台管理</title>
  {{ style('semantic/dist/semantic.min') }}
  {{ style('style/token') }}
</head>
<body>
  <div class="pege background">
    <div class="ui large borderless menu">
      <a class="item" href="/">
        <img src="/logo.jpg" />
        <h2 class="ui header" style="margin-top: 0; margin-left: 15px; font-size: 20px;">管理平台</h2>
      </a>
    </div>
    <div class="ui center aligned grid" style="margin-top: 5%;">
      <div class="six wide column">
        <h2 class="ui teal image header">
          {{-- <img src="/logo.jpg" class="image"> --}}
          <div class="content">
            Log-in to your account
          </div>
        </h2>
        @if(flashMessage('message'))
          @!component('components.alert', {
            type: [flashMessage('type')],
            header: [flashMessage('header')],
            message: [flashMessage('message')]
          })
        @endif
        <form class="ui large form" method="post" action="{{ route }}">
          <div class="ui segment">
            <div class="field">
              <div class="ui left icon input">
                <i class="user icon"></i>
                <input type="text" name="phone" placeholder="" />
              </div>
            </div>
            <div class="field">
              <div class="ui left icon input">
                <i class="lock icon"></i>
                <input type="password" name="password" placeholder="" />
              </div>
            </div>
            {{ csrfField() }}
            <button class="ui fluid large teal submit button" value="0" type="submit" name="submit">Login</button>
          </div>
        </form>
      </div>
    </div>
  </div>
  {{ script('https://apps.bdimg.com/libs/jquery/2.1.1/jquery.min.js') }}
  {{ script('semantic/dist/semantic.min') }}
</body>
</html>
